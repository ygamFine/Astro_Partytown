---
// 热门产品展示组件 - 移动端优化版本
---
<section class="hot-products">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">HOT RECOMMENDED PRODUCTS</h2>
      <p class="section-description">From the procurement of raw materials to assembly of products, we are strict in every process to strictly achieve the international quality management system and product quality.</p>
    </div>
    
    <div class="products-grid">
      <div class="product-item">
        <div class="product-image">
          <img src="/skid1.webp" alt="滑移装载机" loading="lazy">
        </div>
        <h3 class="product-name">Wheeled Skid Steer Loader</h3>
        <a href="/products/skid-steer-loader" class="product-button primary">View Details</a>
      </div>
      
      <div class="product-item">
        <div class="product-image">
          <img src="/backhoe.webp" alt="挖掘装载机" loading="lazy">
        </div>
        <h3 class="product-name">Backhoe Loader</h3>
        <a href="/products/backhoe-loader" class="product-button">View Details</a>
      </div>
      
      <div class="product-item">
        <div class="product-image">
          <img src="/telescopic.webp" alt="伸缩臂叉车" loading="lazy">
        </div>
        <h3 class="product-name">Telescopic Handler</h3>
        <a href="/products/telescopic-handler" class="product-button">View Details</a>
      </div>
    </div>
    
    <!-- 轮播指示器 -->
    <div class="carousel-indicators">
      <button class="indicator active" aria-label="当前产品组"></button>
      <button class="indicator" aria-label="切换产品组"></button>
      <button class="indicator" aria-label="切换产品组"></button>
      <button class="indicator" aria-label="切换产品组"></button>
      <button class="indicator" aria-label="切换产品组"></button>
    </div>
  </div>
</section>

<style>
  /* 基础样式 */
  .hot-products {
    width: 100%;
    background: white;
    padding: 2.5rem 0;
  }

  .container {
    max-width: 75rem;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* 标题区域 */
  .section-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .section-title {
    font-size: 1.5rem;
    font-weight: 800;
    color: #111827; /* gray-900 */
    margin-bottom: 0.5rem;
  }

  .section-description {
    color: #4b5563; /* gray-600 */
    font-size: 1rem;
    max-width: 32rem;
    margin: 0 auto;
    line-height: 1.6;
  }

  @media (min-width: 1024px) {
    .section-title {
      font-size: 1.875rem;
    }

    .section-description {
      font-size: 1rem;
    }
  }

  /* 产品网格 */
  .products-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  @media (min-width: 768px) {
    .products-grid {
      gap: 1.5rem;
    }
  }

  /* 产品项 */
  .product-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 8rem;
    padding: 1rem;
    border-radius: 0.5rem;
    transition: transform 0.2s ease;
  }

  .product-item:hover {
    transform: translateY(-2px);
  }

  @media (min-width: 768px) {
    .product-item {
      width: 10rem;
      padding: 1.5rem;
    }
  }

  @media (max-width: 480px) {
    .product-item {
      width: 7rem;
      padding: 0.75rem;
    }
  }

  /* 产品图片 */
  .product-image {
    width: 100%;
    margin-bottom: 0.75rem;
    border-radius: 0.375rem;
    overflow: hidden;
  }

  .product-image img {
    width: 100%;
    height: 5rem;
    object-fit: cover;
    transition: transform 0.2s ease;
  }

  .product-item:hover .product-image img {
    transform: scale(1.05);
  }

  @media (min-width: 768px) {
    .product-image img {
      height: 6rem;
    }
  }

  @media (max-width: 480px) {
    .product-image img {
      height: 4rem;
    }
  }

  /* 产品名称 */
  .product-name {
    font-weight: 600;
    text-align: center;
    font-size: 0.875rem;
    line-height: 1.3;
    margin-bottom: 0.75rem;
    color: #111827;
    min-height: 2.6rem;
    display: flex;
    align-items: center;
  }

  @media (min-width: 768px) {
    .product-name {
      font-size: 1rem;
    }
  }

  @media (max-width: 480px) {
    .product-name {
      font-size: 0.75rem;
      min-height: 2.25rem;
    }
  }

  /* 产品按钮 */
  .product-button {
    padding: 0.25rem 0.75rem;
    border-radius: 0.25rem;
    font-size: 0.75rem;
    font-weight: 700;
    text-align: center;
    transition: all 0.2s ease;
    text-decoration: none;
    display: inline-block;
    min-height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #374151; /* gray-800 */
    color: white;
  }

  .product-button:hover {
    background-color: #1f2937; /* gray-900 */
    transform: translateY(-1px);
  }

  .product-button.primary {
    background-color: #dc2626; /* red-600 */
    color: white;
  }

  .product-button.primary:hover {
    background-color: #b91c1c; /* red-700 */
  }

  @media (min-width: 768px) {
    .product-button {
      padding: 0.25rem 1rem;
      font-size: 0.75rem;
    }
  }

  @media (max-width: 768px) {
    .product-button {
      min-height: 44px; /* 移动端触摸友好 */
      touch-action: manipulation;
    }
  }

  /* 轮播指示器 */
  .carousel-indicators {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1rem;
  }

  .indicator {
    width: 0.625rem;
    height: 0.625rem;
    border-radius: 50%;
    border: none;
    cursor: pointer;
    transition: all 0.2s ease;
    background-color: #d1d5db; /* gray-300 */
  }

  .indicator.active {
    background-color: #dc2626; /* red-600 */
  }

  .indicator:hover {
    background-color: #9ca3af; /* gray-400 */
  }

  .indicator.active:hover {
    background-color: #b91c1c; /* red-700 */
  }

  @media (max-width: 768px) {
    .indicator {
      min-width: 44px;
      min-height: 44px;
      touch-action: manipulation;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .indicator::before {
      content: '';
      width: 0.625rem;
      height: 0.625rem;
      border-radius: 50%;
      background-color: currentColor;
    }

    .indicator {
      background-color: transparent;
      color: #d1d5db;
    }

    .indicator.active {
      color: #dc2626;
    }

    .indicator:hover {
      color: #9ca3af;
    }

    .indicator.active:hover {
      color: #b91c1c;
    }
  }

  /* 动画效果 */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hot-products {
    animation: fadeIn 0.6s ease-out;
  }

  /* 性能优化 */
  .product-image img {
    transform: translate3d(0, 0, 0);
    backface-visibility: hidden;
  }

  .product-item {
    contain: layout style;
  }

  /* 无障碍性优化 */
  @media (prefers-reduced-motion: reduce) {
    .product-item,
    .product-image img,
    .product-button,
    .indicator {
      transition: none;
    }

    .hot-products {
      animation: none;
    }
  }

  /* 高对比度模式支持 */
  @media (prefers-contrast: high) {
    .product-button {
      border: 2px solid currentColor;
    }

    .indicator {
      border: 2px solid currentColor;
    }
  }
</style> 