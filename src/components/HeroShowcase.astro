---
const mainProduct = {
  src: '/main-product.svg'
};
const mobileBanner = {
  src: '/images/mobile-banner-ultra-fast.svg',
  alt: '专业滑移装载机制造商',
  width: 375,
  height: 200
}
---

<section class="relative bg-white md:pt-24 md:pb-24 overflow-hidden mobile-hero speed-critical">
  <!-- 🚀 移动端Banner - Speed Index极致优化 -->
  <div class="md:hidden mobile-banner-container mobile-critical speed-critical">
    <!-- 立即显示的CSS背景 -->
    <div class="mobile-banner-instant-bg speed-critical"></div>
    <!-- 关键图片 - 立即加载 -->
    <img 
      style="margin: 0 auto; opacity: 1; visibility: visible;"
      src={mobileBanner.src} 
      alt={mobileBanner.alt}
      width={mobileBanner.width}
      height={mobileBanner.height}
      class="mobile-banner speed-critical"
      loading="eager"
      fetchpriority="high"
      decoding="sync"
    />
  </div>

  <!-- 桌面端内容 -->
  <div class="hidden md:block container mx-auto px-4 z-10 relative mobile-hide">
    <div class="grid md:grid-cols-2 gap-8 items-center">
      <!-- 左侧内容 -->
      <div class="text-center md:text-left">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-extrabold text-gray-800 leading-tight tracking-tighter">
          滑移装载机 <br class="hidden md:block"/>
          <span class="text-red-600">引领行业未来</span>
        </h1>
        <p class="mt-6 text-lg md:text-xl text-gray-700 max-w-md mx-auto md:mx-0">
          致力于提供高效、可靠的工程机械解决方案。
        </p>
        <div class="mt-8 flex justify-center md:justify-start space-x-4">
          <a href="/products" class="bg-red-600 text-white px-8 py-3 rounded-lg font-bold text-lg hover:bg-red-700 transition-transform duration-300 transform hover:scale-105">
            探索产品
          </a>
          <a href="/contact" class="bg-gray-200 text-gray-800 px-8 py-3 rounded-lg font-bold text-lg hover:bg-gray-300 transition-transform duration-300 transform hover:scale-105">
            联系我们
          </a>
        </div>
      </div>

      <!-- 右侧图片 -->
      <div class="relative">
        <div class="relative w-full max-w-lg mx-auto md:max-w-none">
          <img src={mainProduct.src} alt="YN-S70滑移装载机" width="600" height="400" class="w-full h-auto object-contain relative z-10">
          <!-- 背景装饰图形 -->
          <div class="absolute -top-10 -right-10 w-40 h-40 bg-red-500 rounded-full opacity-10"></div>
          <div class="absolute -bottom-20 -left-20 w-48 h-48 bg-blue-500 rounded-full opacity-10"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* 🚀 Speed Index关键样式 - 立即显示 */
  .speed-critical {
    opacity: 1 !important;
    visibility: visible !important;
    display: block !important;
    transform: none !important;
    animation: none !important;
    transition: none !important;
    will-change: auto !important;
  }

  /* 移动端Banner容器 - 极致优化 */
  .mobile-banner-container {
    width: 100%;
    margin: 0;
    padding: 0;
    line-height: 0;
    contain: layout style paint;
    will-change: auto;
  }
  
  /* 关键Banner背景 - 立即显示 */
  .mobile-banner-instant-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 200px;
    z-index: 1;
    background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 20px;
    font-weight: bold;
    opacity: 1;
    visibility: visible;
  }

  .mobile-banner-instant-bg::after {
    content: "专业工程机械";
  }
  
  /* 关键Banner图片 - 零延迟显示 */
  .mobile-banner {
    display: block;
    width: 100%;
    height: auto;
    margin: 0;
    padding: 0;
    border: none;
    outline: none;
    vertical-align: top;
    image-rendering: optimizeSpeed;
    image-rendering: -webkit-optimize-contrast;
    image-rendering: crisp-edges;
    opacity: 1;
    visibility: visible;
    z-index: 2;
    position: relative;
    max-height: 200px;
    object-fit: cover;
    contain: layout style paint;
  }
  
  /* 移动端响应式优化 */
  @media (max-width: 768px) {
    .mobile-banner-container {
      position: relative;
      height: 200px;
      overflow: hidden;
    }
    
    .mobile-banner {
      width: 100%;
      height: 200px;
      object-fit: cover;
      object-position: center;
    }
  }
  
  @media (max-width: 480px) {
    .mobile-banner-container {
      height: 180px;
    }
    
    .mobile-banner {
      height: 180px;
    }
    
    .mobile-banner-instant-bg {
      height: 180px;
      font-size: 18px;
    }
  }
  
  @media (max-width: 320px) {
    .mobile-banner-container {
      height: 160px;
    }
    
    .mobile-banner {
      height: 160px;
    }
    
    .mobile-banner-instant-bg {
      height: 160px;
      font-size: 16px;
    }
  }
</style>

<!-- 🚀 Speed Index极致优化 - 移除本地JavaScript，由Layout处理 --> 